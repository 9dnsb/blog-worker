# MongoDB connection string (same as your Payload backend)
DATABASE_URI=mongodb+srv://...

# OpenAI API key
OPENAI_API_KEY=sk-...

# Shared secret for authenticating requests from Vercel
# Generate with: openssl rand -hex 32
WORKER_SECRET=your-secret-here

# Port (Render sets this automatically)
PORT=3001
